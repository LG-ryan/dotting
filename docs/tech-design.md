# DOTTING 기술 설계 문서

> **버전**: 1.1 (리뷰 피드백 반영)  
> **작성일**: 2026년 1월 10일  
> **상태**: 실행용

---

## 목차

1. [개요](#1-개요)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [핵심 기술 과제](#3-핵심-기술-과제)
4. [입력 방식 설계](#4-입력-방식-설계)
5. [AI 인터뷰어 설계](#5-ai-인터뷰어-설계)
6. [데이터 모델](#6-데이터-모델)
7. [성능 최적화](#7-성능-최적화)
8. [보안 및 개인정보](#8-보안-및-개인정보)

---

# 1. 개요

## 1.1 기술 스택 요약

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        DOTTING 기술 스택                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   FRONTEND                                                              │
│   ────────────────────────────────────                                  │
│   • Next.js 14 (App Router)                                             │
│   • TypeScript                                                          │
│   • Tailwind CSS + Shadcn/ui                                            │
│                                                                         │
│   BACKEND                                                               │
│   ────────────────────────────────────                                  │
│   • Next.js API Routes (Edge Functions)                                 │
│   • Vercel AI SDK                                                       │
│                                                                         │
│   DATABASE & STORAGE                                                    │
│   ────────────────────────────────────                                  │
│   • Supabase (PostgreSQL + Auth + Storage)                              │
│                                                                         │
│   AI / ML                                                               │
│   ────────────────────────────────────                                  │
│   • OpenAI GPT-4o (질문 생성, 스토리 정리)                              │
│   • OpenAI Whisper (음성→텍스트)                                        │
│                                                                         │
│   INFRA                                                                 │
│   ────────────────────────────────────                                  │
│   • Vercel (배포, CDN, Edge)                                            │
│   • Supabase Storage (오디오 파일)                                      │
│                                                                         │
│   PAYMENT                                                               │
│   ────────────────────────────────────                                  │
│   • Toss Payments                                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 1.2 설계 원칙

| 원칙 | 설명 |
|------|------|
| **1인 개발 최적화** | 복잡도 최소화, 모노리포, 풀스택 프레임워크 |
| **점진적 복잡화** | MVP는 단순하게, 필요시 확장 |
| **실패 허용** | 모든 외부 API 호출에 재시도/폴백 로직 |
| **사용자 중심** | 기술적 완벽함보다 UX 우선 |

---

# 2. 시스템 아키텍처

## 2.1 전체 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   [자녀]                                [부모님/응답자]                  │
│     │                                        │                          │
│     │ 1. 회원가입/결제                        │                          │
│     ▼                                        │                          │
│   ┌──────────┐                               │                          │
│   │ Dashboard│                               │                          │
│   │  Page    │────── 2. 링크 생성 ──────────▶│                          │
│   └──────────┘                               │                          │
│     │                                        │ 3. 링크 클릭             │
│     │                                        ▼                          │
│     │                                  ┌──────────┐                     │
│     │                                  │ Interview│                     │
│     │                                  │   Page   │                     │
│     │                                  └──────────┘                     │
│     │                                        │                          │
│     │                         4. 음성/텍스트 입력                        │
│     │                                        ▼                          │
│     │                                  ┌──────────┐                     │
│     │                                  │  API     │                     │
│     │                                  │ Routes   │                     │
│     │                                  └──────────┘                     │
│     │                                        │                          │
│     │                    ┌───────────────────┼───────────────────┐      │
│     │                    │                   │                   │      │
│     │                    ▼                   ▼                   ▼      │
│     │              ┌──────────┐       ┌──────────┐       ┌──────────┐  │
│     │              │ Whisper  │       │  GPT-4o  │       │ Supabase │  │
│     │              │(음성→텍스트)│     │(질문생성)│       │ (저장)   │  │
│     │              └──────────┘       └──────────┘       └──────────┘  │
│     │                    │                   │                          │
│     │                    └───────────────────┘                          │
│     │                             │                                     │
│     │              5. AI 응답 / 다음 질문                               │
│     │                             │                                     │
│     │                             ▼                                     │
│     │ ◄───── 6. 진행상황 확인 ─────                                    │
│     │                                                                   │
│     │              7. 완료 후 PDF 생성                                  │
│     ▼                                                                   │
│   ┌──────────┐                                                         │
│   │  PDF     │                                                         │
│   │ Download │                                                         │
│   └──────────┘                                                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 2.2 페이지 구조

```
/
├── (marketing)
│   └── page.tsx              # 랜딩 페이지
│
├── (auth)
│   ├── login/page.tsx        # 로그인
│   └── signup/page.tsx       # 회원가입
│
├── (dashboard)
│   ├── layout.tsx            # 대시보드 레이아웃
│   ├── page.tsx              # 프로젝트 목록
│   ├── new/page.tsx          # 새 프로젝트 생성
│   └── [projectId]/
│       ├── page.tsx          # 프로젝트 상세
│       ├── stories/page.tsx  # 이야기 목록
│       └── preview/page.tsx  # 미리보기
│
├── (interview)
│   └── [token]/
│       └── page.tsx          # 인터뷰 페이지 (응답자용)
│
└── api/
    ├── auth/                 # 인증 API
    ├── projects/             # 프로젝트 CRUD
    ├── interview/            # 인터뷰 관련
    │   ├── transcribe/       # 음성→텍스트
    │   └── generate-question/# 질문 생성
    ├── stories/              # 이야기 CRUD
    └── pdf/                  # PDF 생성
```

---

# 3. 핵심 기술 과제

## 3.1 과제 1: Latency (응답 지연)

### 문제

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   음성 입력 시 지연 문제                                                │
│                                                                         │
│   사용자 발화 종료                                                      │
│        │                                                                │
│        ├── 1. 오디오 업로드 (~0.5초)                                    │
│        ├── 2. Whisper 변환 (~2-3초)                                     │
│        ├── 3. GPT-4o 응답 생성 (~2-3초)                                 │
│        │                                                                │
│        ▼                                                                │
│   총 지연: 5~7초                                                        │
│                                                                         │
│   ⚠️ 시니어는 3초 이상 침묵 시 "고장났나?" 생각                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 해결책

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   해결책 1: 즉시 피드백                                                 │
│   ────────────────────────────────────                                  │
│   • 녹음 종료 즉시: "네, 잘 들었어요 ● ● ●"                            │
│   • 애니메이션: 로딩 점 (●→●●→●●●)                                      │
│   • 음성 피드백 (옵션): "잠시만요..." TTS                               │
│                                                                         │
│   해결책 2: 스트리밍                                                    │
│   ────────────────────────────────────                                  │
│   • GPT 응답을 스트리밍으로 받아 실시간 표시                            │
│   • 첫 토큰 도착까지 1초 이내                                           │
│                                                                         │
│   해결책 3: 병렬 처리                                                   │
│   ────────────────────────────────────                                  │
│   • Whisper 변환 중 이전 대화 컨텍스트 준비                             │
│   • DB 저장과 API 호출 병렬화                                           │
│                                                                         │
│   해결책 4: 텍스트 입력 권장                                            │
│   ────────────────────────────────────                                  │
│   • 텍스트 입력 시 Whisper 과정 생략 → 2~3초 절약                       │
│   • "타이핑이 편하시면 여기에 입력하세요" 옵션 제공                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 구현 코드 예시

```typescript
// 스트리밍 응답 예시
async function generateQuestion(context: ConversationContext) {
  const stream = await openai.chat.completions.create({
    model: 'gpt-4o',
    messages: buildMessages(context),
    stream: true,
  });

  // 첫 토큰 도착 시 즉시 UI 업데이트
  for await (const chunk of stream) {
    const content = chunk.choices[0]?.delta?.content || '';
    yield content; // 실시간 전달
  }
}
```

## 3.1.5 과제 1.5: 녹음 시간 제한 (Vercel 타임아웃)

### 문제

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   Vercel Serverless 타임아웃 문제                                       │
│                                                                         │
│   ────────────────────────────────────                                  │
│                                                                         │
│   문제:                                                                 │
│   • Vercel Serverless: 기본 10초, Pro 60초 타임아웃                     │
│   • 부모님: 30분+ 연속 이야기 가능                                      │
│   • 한 번에 업로드 → 타임아웃 발생                                      │
│                                                                         │
│   UX 문제:                                                              │
│   • "3분 녹음 제한" → 이야기 흐름 끊김                                  │
│   • 부모님: "옛날에 전쟁 났을 때..." → "띵! 시간 초과" = 감동 깨짐     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 해결책: 클라이언트 청크 녹음 (권장)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   해결책: 클라이언트에서 청크로 녹음 + 순차 업로드                      │
│                                                                         │
│   ────────────────────────────────────                                  │
│                                                                         │
│   [사용자 경험]                                                         │
│   • 사용자는 끊김 없이 계속 말함                                        │
│   • 백그라운드에서 3분마다 청크 저장 + 업로드                           │
│   • 사용자가 "완료" 누르면 마지막 청크 업로드                           │
│                                                                         │
│   [기술 구현]                                                           │
│                                                                         │
│   1. MediaRecorder로 녹음 시작                                          │
│   2. 3분마다 ondataavailable 이벤트 발생                                │
│   3. 각 청크를 Supabase Storage에 업로드                                │
│   4. 사용자가 "완료" 누르면:                                            │
│      - 마지막 청크 업로드                                               │
│      - 서버에서 모든 청크 합치기                                        │
│      - Whisper API 호출                                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 구현 코드 예시

```typescript
// 클라이언트: 청크 녹음
const CHUNK_DURATION = 3 * 60 * 1000; // 3분

function startChunkedRecording(projectId: string, storyId: string) {
  const mediaRecorder = new MediaRecorder(stream);
  let chunkIndex = 0;
  
  mediaRecorder.ondataavailable = async (event) => {
    if (event.data.size > 0) {
      // 각 청크를 백그라운드에서 업로드
      await uploadChunk(projectId, storyId, chunkIndex, event.data);
      chunkIndex++;
    }
  };
  
  // 3분마다 청크 생성
  mediaRecorder.start(CHUNK_DURATION);
  
  return {
    stop: async () => {
      mediaRecorder.stop();
      // 서버에 "녹음 완료" 알림 → 청크 합치기 + Whisper 호출
      await finalizeRecording(projectId, storyId);
    }
  };
}

// 서버: 청크 합치기
async function finalizeRecording(projectId: string, storyId: string) {
  // 1. 모든 청크 파일 가져오기
  const chunks = await getChunks(projectId, storyId);
  
  // 2. 청크 합치기 (ffmpeg 또는 간단한 concat)
  const mergedAudio = await mergeChunks(chunks);
  
  // 3. Whisper API 호출
  const transcript = await whisperTranscribe(mergedAudio);
  
  return transcript;
}
```

### 대안: 함께하기 모드에서는 덜 문제

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   함께하기 모드에서의 완화                                              │
│                                                                         │
│   ────────────────────────────────────                                  │
│                                                                         │
│   MVP 메인 시나리오 = 함께하기 모드                                     │
│   • 자녀가 옆에서 진행                                                  │
│   • 자녀: "잠깐, 저장할게요~" 가능                                      │
│   • 질문 단위로 끊어서 녹음 가능                                        │
│                                                                         │
│   → 3분 제한이 덜 문제됨                                                │
│   → 하지만 청크 업로드는 구현 권장 (혼자 모드 대비)                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 3.2 과제 2: 발화 종료 감지 (음성 입력 시)

### 문제

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   문제 A: 너무 빨리 끊음                                                │
│   ────────────────────────────────────                                  │
│   부모님: "그때가... 음..." (생각 중)                                   │
│   시스템: (2초 침묵 감지) → 녹음 종료                                   │
│   부모님: "내 말 끊네?"                                                 │
│                                                                         │
│   문제 B: 너무 늦게 끊음                                                │
│   ────────────────────────────────────                                  │
│   부모님: (말 끝냄)                                                     │
│   [10초 동안 TV 소리 녹음 중...]                                        │
│   → API 비용 낭비 + 인식 오류                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 해결책: 버튼 방식 (MVP)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   MVP: 수동 버튼 방식                                                   │
│   ────────────────────────────────────                                  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────┐              │
│   │                                                     │              │
│   │   AI: "어린 시절 가장 기억에 남는 순간은?"          │              │
│   │                                                     │              │
│   │           ┌───────────────────┐                     │              │
│   │           │                   │                     │              │
│   │           │   🎤 말하기       │  ← 누르고 있는 동안 녹음 │
│   │           │                   │                     │              │
│   │           └───────────────────┘                     │              │
│   │                                                     │              │
│   │   "버튼을 누르고 말씀해주세요"                      │              │
│   │                                                     │              │
│   └─────────────────────────────────────────────────────┘              │
│                                                                         │
│   • Push-to-Talk 방식 (누르고 있는 동안만 녹음)                        │
│   • 또는 Toggle 방식 (한 번 누르면 시작, 다시 누르면 종료)             │
│   • 시각적 피드백: 녹음 중 큰 빨간 원 ●                                │
│                                                                         │
│   장점:                                                                 │
│   ────────────────────────────────────                                  │
│   • 사용자가 완전히 제어                                                │
│   • 실수로 긴 침묵 녹음 방지                                            │
│   • 구현 단순                                                           │
│                                                                         │
│   단점:                                                                 │
│   ────────────────────────────────────                                  │
│   • 자연스러운 대화 느낌 감소                                           │
│   • 시니어에게 버튼 조작 교육 필요                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 향후 개선 (v2.0)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   v2.0: 스마트 감지 (옵션)                                              │
│   ────────────────────────────────────                                  │
│                                                                         │
│   1. VAD (Voice Activity Detection)                                     │
│      • 웹 Audio API로 음성 구간 감지                                    │
│      • 3초 이상 침묵 시 확인 질문                                       │
│                                                                         │
│   2. 확인 질문                                                          │
│      "더 말씀하실 게 있으신가요?"                                       │
│      [예] → 계속 녹음                                                   │
│      [아니오] → 녹음 종료                                               │
│                                                                         │
│   3. 하이브리드                                                         │
│      • 기본: 버튼 방식                                                  │
│      • 설정에서 자동 감지 ON/OFF                                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 3.3 과제 3: AI 환각 (Hallucination)

### 문제

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   시나리오                                                              │
│   ────────────────────────────────────                                  │
│   부모님: "우리 어머니가 돌아가셨을 때..."                               │
│   AI: "어머니께서 지금 어떻게 지내고 계세요?"                            │
│   부모님: "???"                                                         │
│                                                                         │
│   원인                                                                  │
│   ────────────────────────────────────                                  │
│   • 문맥 이해 실패                                                      │
│   • 과거/현재 시제 혼동                                                 │
│   • 존재하지 않는 정보 생성                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 해결책

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   해결책 1: Safety Prompt                                               │
│   ────────────────────────────────────                                  │
│                                                                         │
│   시스템 프롬프트에 포함:                                               │
│   ───────────────────────────────────────────────────────               │
│   "당신은 인터뷰어입니다. 다음 규칙을 반드시 지키세요:                  │
│   1. 사용자가 명시적으로 말하지 않은 사실을 가정하지 마세요.            │
│   2. 확실하지 않으면 '제가 잘 이해했는지 확인해도 될까요?'라고          │
│      물어보세요.                                                        │
│   3. '돌아가셨다', '떠났다' 등의 표현이 나오면 해당 사실을 기억하고     │
│      이후 질문에서 현재 시제로 묻지 마세요.                             │
│   4. 민감한 주제(죽음, 이별, 질병)는 조심스럽게 다루세요."              │
│                                                                         │
│   해결책 2: 컨텍스트 관리                                               │
│   ────────────────────────────────────                                  │
│                                                                         │
│   • 대화 히스토리 전체를 컨텍스트로 유지                                │
│   • 중요 정보 추출 및 저장 (별도 필드)                                  │
│     - 가족 구성원 상태 (생존/사망)                                      │
│     - 민감한 사건 (이혼, 질병 등)                                       │
│                                                                         │
│   해결책 3: 질문 검토 (자녀)                                            │
│   ────────────────────────────────────                                  │
│                                                                         │
│   • 선택 옵션: 자녀가 AI 질문을 미리 검토                               │
│   • AI가 생성한 질문 → 자녀 승인 → 부모님에게 전달                      │
│   • Human-in-the-loop으로 품질 보장                                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 3.4 과제 4: 스토리지 비용 (오디오)

### 문제

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   오디오 저장 비용 예측                                                 │
│   ────────────────────────────────────                                  │
│                                                                         │
│   가정:                                                                 │
│   • 1건당 평균 90분 녹음 (30분 × 3세션)                                 │
│   • 오디오 파일: 약 100MB/건                                            │
│   • Supabase Storage: $0.021/GB/월                                      │
│                                                                         │
│   비용 계산:                                                            │
│   • Y1: 400건 × 100MB = 40GB → $0.84/월                                 │
│   • Y2: 1,900건 누적 = 190GB → $3.99/월                                 │
│   • Y3: 5,900건 누적 = 590GB → $12.39/월                                │
│                                                                         │
│   ⚠️ "평생 보관" 약속 시 비용 계속 증가                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 해결책

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   옵션 1: 텍스트만 기본 보관                                            │
│   ────────────────────────────────────                                  │
│   • 음성→텍스트 변환 후 텍스트만 저장 (기본)                            │
│   • 오디오 원본은 결제 완료 후 30일간만 보관                            │
│   • 이후 자동 삭제 (약관 명시)                                          │
│                                                                         │
│   옵션 2: 오디오 보관은 유료 옵션                                       │
│   ────────────────────────────────────                                  │
│   • Premium 패키지: 오디오 원본 1년 보관 포함                           │
│   • 추가 구매: 영구 보관 +2만원                                         │
│                                                                         │
│   옵션 3: 콜드 스토리지 이관                                            │
│   ────────────────────────────────────                                  │
│   • 30일 후 AWS Glacier 등으로 이관                                     │
│   • 저렴한 비용으로 장기 보관                                           │
│   • 다운로드 시 지연 있음 (약관 명시)                                   │
│                                                                         │
│   MVP 결정: 옵션 1 (텍스트만 기본 보관)                                 │
│   ────────────────────────────────────                                  │
│   • 간단한 구현                                                         │
│   • 비용 예측 가능                                                      │
│   • 나중에 유료 옵션 추가 가능                                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

# 4. 입력 방식 설계

## 4.1 하이브리드 입력

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   MVP: 음성 + 텍스트 모두 지원                                          │
│                                                                         │
│   ┌───────────────────────────────────────────────────────────────┐    │
│   │                                                               │    │
│   │   AI: "어린 시절 가장 기억에 남는 순간은?"                    │    │
│   │                                                               │    │
│   │   ┌─────────────────────────────────────────────────────────┐│    │
│   │   │                                                         ││    │
│   │   │  (텍스트 입력 영역)                                     ││    │
│   │   │                                                         ││    │
│   │   │  편하게 작성해주세요...                                 ││    │
│   │   │                                                         ││    │
│   │   └─────────────────────────────────────────────────────────┘│    │
│   │                                                               │    │
│   │   ───────────── 또는 ─────────────                            │    │
│   │                                                               │    │
│   │           ┌───────────────────┐                               │    │
│   │           │   🎤 음성으로     │                               │    │
│   │           │      말하기       │                               │    │
│   │           └───────────────────┘                               │    │
│   │                                                               │    │
│   └───────────────────────────────────────────────────────────────┘    │
│                                                                         │
│   • 사용자가 자유롭게 선택                                              │
│   • 질문마다 다른 방식 사용 가능                                        │
│   • 시니어: 음성 권장 / 젊은 층: 텍스트 권장                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 4.2 음성 입력 플로우

```
사용자 ──▶ [말하기 버튼 클릭]
              │
              ▼
         [녹음 시작] ──▶ 화면: 🔴 녹음 중...
              │
              │ (사용자 발화)
              ▼
         [말하기 버튼 다시 클릭 or 3초 후 자동 종료]
              │
              ▼
         [오디오 Blob 생성]
              │
              ├──▶ 화면: "● ● ● 듣고 있어요..."
              │
              ▼
         [Supabase Storage 업로드]
              │
              ▼
         [Whisper API 호출]
              │
              ▼
         [텍스트 변환 완료]
              │
              ├──▶ 화면: 변환된 텍스트 표시
              │     "수정이 필요하면 수정해주세요"
              ▼
         [확인 버튼]
              │
              ▼
         [GPT-4o 호출 → 다음 질문 생성]
              │
              ▼
         화면: 다음 질문 표시
```

## 4.3 텍스트 입력 플로우

```
사용자 ──▶ [텍스트 입력]
              │
              ▼
         [제출 버튼 클릭]
              │
              ├──▶ 화면: "● ● ● 생각 중..."
              │
              ▼
         [GPT-4o 호출 → 다음 질문 생성]
              │
              ▼
         화면: 다음 질문 표시

※ Whisper 과정 없음 → 2~3초 절약
```

---

# 5. AI 인터뷰어 설계

## 5.1 프롬프트 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   System Prompt (고정)                                                  │
│   ────────────────────────────────────                                  │
│   당신은 따뜻하고 공감적인 인터뷰어입니다.                              │
│   사용자의 인생 이야기를 듣고 기록하는 역할입니다.                      │
│                                                                         │
│   규칙:                                                                 │
│   1. 한국어로 대화합니다.                                               │
│   2. 존댓말을 사용합니다.                                               │
│   3. 답변에 공감하고, 자연스럽게 후속 질문을 합니다.                    │
│   4. 한 번에 하나의 질문만 합니다.                                      │
│   5. 민감한 주제는 조심스럽게 다룹니다.                                 │
│   6. 사용자가 말하지 않은 사실을 가정하지 않습니다.                     │
│   7. 짧고 간결하게 질문합니다.                                          │
│                                                                         │
│   ────────────────────────────────────                                  │
│                                                                         │
│   Context (동적)                                                        │
│   ────────────────────────────────────                                  │
│   • 이전 대화 히스토리 (전체)                                           │
│   • 인터뷰 대상 정보 (이름, 관계 등)                                    │
│   • 진행 상태 (몇 번째 질문인지)                                        │
│   • 추출된 중요 정보 (가족 상태 등)                                     │
│                                                                         │
│   ────────────────────────────────────                                  │
│                                                                         │
│   User Prompt (매번)                                                    │
│   ────────────────────────────────────                                  │
│   사용자의 답변: "{user_answer}"                                        │
│                                                                         │
│   위 답변에 공감하며 자연스럽게 후속 질문을 해주세요.                   │
│   답변에서 더 깊이 알고 싶은 부분이 있다면 그것에 대해 물어보세요.      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 5.2 Deep Dive 로직

```typescript
// Deep Dive 결정 로직
function shouldDeepDive(answer: string, questionCount: number): boolean {
  // 답변이 충분히 길면 Deep Dive
  if (answer.length > 200) return true;
  
  // 감정적 키워드가 있으면 Deep Dive
  const emotionalKeywords = ['행복', '슬펐', '힘들었', '기뻤', '눈물'];
  if (emotionalKeywords.some(k => answer.includes(k))) return true;
  
  // 이미 3번 Deep Dive 했으면 새 주제로
  if (questionCount >= 3) return false;
  
  return false;
}
```

## 5.3 주제 관리

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   기본 주제 풀 (시드 질문)                                              │
│   ────────────────────────────────────                                  │
│                                                                         │
│   1. 어린 시절                                                          │
│      • 가장 행복했던 순간                                               │
│      • 어린 시절 살던 동네                                              │
│      • 부모님과의 추억                                                  │
│                                                                         │
│   2. 학창 시절                                                          │
│      • 가장 친했던 친구                                                 │
│      • 꿈과 진로                                                        │
│                                                                         │
│   3. 청년 시절                                                          │
│      • 첫 직장                                                          │
│      • 연애와 결혼                                                      │
│                                                                         │
│   4. 가족                                                               │
│      • 자녀가 태어났을 때                                               │
│      • 가장 자랑스러운 순간                                             │
│                                                                         │
│   5. 인생 교훈                                                          │
│      • 가장 큰 실패와 극복                                              │
│      • 후대에 전하고 싶은 말                                            │
│                                                                         │
│   ────────────────────────────────────                                  │
│                                                                         │
│   동작 방식:                                                            │
│   • 시드 질문으로 시작                                                  │
│   • 답변에 따라 Deep Dive (2~3회)                                       │
│   • Deep Dive 완료 후 다음 시드 질문                                    │
│   • 사용자가 원하면 주제 건너뛰기 가능                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

# 6. 데이터 모델

## 6.1 Supabase 스키마

```sql
-- 사용자 (자녀 = 구매자)
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  name TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 프로젝트 (책 1권 = 프로젝트 1개)
CREATE TABLE projects (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  title TEXT NOT NULL,
  
  -- 인터뷰 대상 정보
  subject_name TEXT,
  subject_relation TEXT, -- '부모님', '본인', '반려동물' 등
  
  -- 모드
  mode TEXT DEFAULT 'relaxed', -- 'relaxed', 'dday', 'together'
  
  -- 상태
  status TEXT DEFAULT 'draft', -- 'draft', 'in_progress', 'completed', 'paid'
  
  -- 공유 토큰 (응답자용 링크)
  share_token TEXT UNIQUE,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 이야기 (질문-답변 1세트)
CREATE TABLE stories (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  project_id UUID REFERENCES projects(id),
  
  -- 질문
  question TEXT NOT NULL,
  question_topic TEXT, -- '어린시절', '가족' 등
  
  -- 답변
  answer_text TEXT,
  answer_audio_url TEXT, -- 음성 원본 (옵션)
  
  -- 입력 방식
  input_type TEXT DEFAULT 'text', -- 'text', 'voice'
  
  -- AI 정리된 이야기
  story_content TEXT,
  
  -- 순서
  order_index INT,
  
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 대화 히스토리 (AI 컨텍스트용)
CREATE TABLE conversation_history (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  story_id UUID REFERENCES stories(id),
  role TEXT NOT NULL, -- 'user', 'assistant'
  content TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 결제
CREATE TABLE payments (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  project_id UUID REFERENCES projects(id),
  amount INT NOT NULL,
  status TEXT DEFAULT 'pending',
  payment_key TEXT, -- Toss Payments
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

## 6.2 Row Level Security (RLS)

```sql
-- users: 본인만 조회/수정
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can view own data" ON users
  FOR SELECT USING (auth.uid() = id);

-- projects: 소유자만 접근
ALTER TABLE projects ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can manage own projects" ON projects
  FOR ALL USING (auth.uid() = user_id);

-- 공유 토큰으로 접근 (응답자용)
CREATE POLICY "Anyone with token can view project" ON projects
  FOR SELECT USING (share_token IS NOT NULL);
```

---

# 7. 성능 최적화

## 7.1 API 응답 최적화

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   1. Edge Functions 사용                                                │
│   ────────────────────────────────────                                  │
│   • Vercel Edge Functions로 지연 최소화                                 │
│   • 사용자 가까운 엣지에서 실행                                         │
│                                                                         │
│   2. 스트리밍                                                           │
│   ────────────────────────────────────                                  │
│   • GPT 응답 스트리밍                                                   │
│   • ReadableStream으로 실시간 전달                                      │
│                                                                         │
│   3. 캐싱                                                               │
│   ────────────────────────────────────                                  │
│   • 정적 콘텐츠 CDN 캐싱                                                │
│   • 질문 템플릿 클라이언트 캐싱                                         │
│                                                                         │
│   4. Lazy Loading                                                       │
│   ────────────────────────────────────                                  │
│   • 이야기 목록 페이지네이션                                            │
│   • 이미지/오디오 지연 로딩                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 7.2 비용 최적화

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   1. 토큰 관리                                                          │
│   ────────────────────────────────────                                  │
│   • 컨텍스트 길이 제한 (최근 10개 대화만)                               │
│   • 불필요한 시스템 프롬프트 최소화                                     │
│                                                                         │
│   2. 모델 선택                                                          │
│   ────────────────────────────────────                                  │
│   • 주력: GPT-4o (품질 우선)                                            │
│   • 경량 작업: GPT-4o-mini (비용 1/10)                                  │
│     - 텍스트 정리, 요약 등                                              │
│                                                                         │
│   3. 오디오 최적화                                                      │
│   ────────────────────────────────────                                  │
│   • 녹음 시 압축 (webm/opus)                                            │
│   • 불필요한 오디오 삭제 정책                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

# 8. 보안 및 개인정보

## 8.1 인증 및 권한

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   1. 인증 (Supabase Auth)                                               │
│   ────────────────────────────────────                                  │
│   • 이메일/비밀번호                                                     │
│   • 카카오 소셜 로그인                                                  │
│   • JWT 기반 세션                                                       │
│                                                                         │
│   2. 권한 분리                                                          │
│   ────────────────────────────────────                                  │
│   • 자녀 (구매자): 전체 권한                                            │
│   • 응답자 (부모님): 공유 링크로 인터뷰만 가능                          │
│     - 로그인 불필요                                                     │
│     - 토큰 기반 접근                                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 8.2 개인정보 보호

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   1. 데이터 암호화                                                      │
│   ────────────────────────────────────                                  │
│   • 전송 중: HTTPS                                                      │
│   • 저장 시: Supabase 기본 암호화                                       │
│   • 민감 필드: 추가 암호화 고려 (v2.0)                                  │
│                                                                         │
│   2. 데이터 보관 정책                                                   │
│   ────────────────────────────────────                                  │
│   • 텍스트: 영구 보관 (기본)                                            │
│   • 오디오: 30일 후 삭제 (기본)                                         │
│   • 계정 삭제 시: 관련 데이터 전부 삭제                                 │
│                                                                         │
│   3. 약관 필수 항목                                                     │
│   ────────────────────────────────────                                  │
│   • 개인정보 수집 동의                                                  │
│   • 음성 데이터 처리 동의                                               │
│   • AI 학습 데이터로 사용 여부 (미사용 명시)                            │
│   • 사망 후 데이터 처리 방침                                            │
│                                                                         │
│   4. 법적 고려사항                                                      │
│   ────────────────────────────────────                                  │
│   • 음성 = 생체 데이터 → 별도 동의 필요                                 │
│   • AI 생성 콘텐츠 저작권 → 사용자 귀속 명시                            │
│   • 잊혀질 권리 보장                                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

# 다음 단계

| # | 항목 | 우선순위 |
|---|------|---------|
| 1 | DB 스키마 생성 (Supabase) | 🔴 |
| 2 | 인증 시스템 구현 | 🔴 |
| 3 | 텍스트 입력 플로우 구현 (음성보다 먼저) | 🔴 |
| 4 | AI 프롬프트 테스트/튜닝 | 🔴 |
| 5 | 음성 입력 플로우 구현 | 🟡 |
| 6 | PDF 생성 구현 | 🟡 |

---

*본 문서는 DOTTING 기술 설계 초안입니다.*  
*실제 구현 시 상세 내용 추가 예정.*
