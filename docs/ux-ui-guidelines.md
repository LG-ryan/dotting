# DOTTING UX/UI Design Guidelines v1.5

> **문서 버전**: v1.5 (실제 랜딩 기준 + 드롭다운 UX)  
> **작성일**: 2026년 1월 18일  
> **최종 수정**: 2026년 1월 18일  
> **상태**: Active  
> **SSOT (Single Source of Truth)**: `globals.css`, `button.tsx`, `dot.tsx`

---

## 📋 v1.5 최종 수정 사항 (2026.01.18)

| 항목 | v1.4 (여전히 큼) | v1.5 (최종) | 이유 |
|------|-----------------|------------|------|
| **Button 크기** | default: 44px, 16px | **default: 40px, 14px** | 실제 랜딩 네비게이션 기준 |
| **Button 체계** | sm: 36px | **sm: 32px** | 랜딩 네비게이션 실측 |
| **삭제 UX** | 중앙 모달 | **드롭다운 카드** | 버튼 근처 표시, 맥락 유지 |
| **삭제 디자인** | Deep Navy | **Red 600** | 위험 행동 명확한 시그널 |

### v1.4의 문제점 (Lead PM 피드백)
- **"버튼 여전히 너무 큼"** → 랜딩 네비게이션이 이미 작았음 (36px 이하)
- **"삭제 모달 여기저기 움직여"** → 중앙 모달은 버튼과 떨어져 있음
- **"UX/UI 너무 별로"** → 평범한 모달, 맥락 단절

### v1.3의 문제점
- 버튼 기본 크기를 56px로 설정 → 모든 버튼이 과도하게 커짐
- 버튼 텍스트를 17px로 설정 → "전송", "이야기 정리하기" 등이 너무 커짐
- 본문 텍스트(읽기용)와 버튼 텍스트(액션용)를 구분하지 않음
- 삭제 모달의 Rose Pink 사용 → 브랜드 톤 위배
- flex-1 사용 → 텍스트 길이 변화 시 레이아웃 shift 발생

---

## 📋 v1.2 변경 사항

| 항목 | v1.1 | v1.2 | 이유 |
|------|------|------|------|
| **점 시스템** | 진행 상태 | **패키지 정체성** | PackageType Enum 도입 |
| **Heritage 차별화** | 없음 | **Micro-elements** | 전체 테마 전환 X, 디테일만 |
| **애니메이션** | Bounce | **Ink Spread** | 게임 X, 유산 O |
| **컬러 전략** | 기본 | **Legacy Respect** | 기존 Amber만으로 금빛 표현 |
| **오디오 UI** | 없음 | **파형 시각화** | 목소리 중심 멀티모달 |

---

## 📖 목차

1. [브랜드 철학](#1-브랜드-철학)
2. [컬러 시스템](#2-컬러-시스템)
3. [타이포그래피](#3-타이포그래피)
4. [시각적 아이덴티티](#4-시각적-아이덴티티)
5. [컴포넌트](#5-컴포넌트)
6. [레이아웃](#6-레이아웃)
7. [애니메이션](#7-애니메이션)
8. [아이콘 & 이미지](#8-아이콘--이미지)
9. [반응형 디자인](#9-반응형-디자인)
10. [접근성](#10-접근성)

---

## 1. 브랜드 철학

### 1.1 핵심 무드

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   브랜드 무드: 따뜻한 편집실 / 조용한 럭셔리 / 책의 질감        │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   🪵 따뜻한 편집실                                              │
│   • 부드러운 조명 아래 책장이 있는 공간                         │
│   • 나무 책상 위에 펼쳐진 원고                                  │
│   • 빈티지 노트와 가죽 표지                                     │
│                                                                 │
│   💎 조용한 럭셔리                                              │
│   • 과시하지 않는 고급스러움                                    │
│   • 시간이 지나도 변하지 않는 가치                              │
│   • 정제된 디테일                                               │
│                                                                 │
│   📚 책의 질감                                                  │
│   • 종이의 부드러운 촉감                                        │
│   • 인쇄물의 은은한 냄새                                        │
│   • 제본의 견고함                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 브랜드 슬로건

| 구분 | 슬로건 |
|------|--------|
| **메인** | "모든 이야기는 계속된다 ●●●" |
| **서브** | "점을 이어 선으로, 선을 모아 이야기로" |
| **히어로** | "소중한 이야기를 한 권의 책으로" |

### 1.3 톤 & 매너

```
✅ DO:
• 따뜻하고 진정성 있는 톤
• 공감과 존중이 담긴 표현
• 시적이면서도 명확한 언어
• "당신"이 아닌 "소중한 이야기" 중심

❌ DON'T:
• 과장된 마케팅 용어 ("최고!", "대박!")
• 기술 중심 표현 ("AI가 자동으로~")
• 조급함을 유발하는 문구 ("지금 안하면 후회!")
• 슬픔이나 죽음 직접 언급
```

### 1.4 핵심 원칙 (v1.2 업데이트)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   토스의 단순함 + 도팅의 온기                                   │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   1. 사용자가 생각하게 하지 마라                                │
│      → 화면당 Primary CTA 1개만                                 │
│                                                                 │
│   2. 기능 없는 장식은 제거                                      │
│      → 점(●●●)을 패키지 정체성 시스템으로 승격                 │
│                                                                 │
│   3. 브랜드 본질은 지킨다                                       │
│      → Serif 폰트 = 책의 질감 (유지)                           │
│      → Amber 컬러 = 따뜻함 (유지)                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.5 절제된 설계 원칙 (v1.2 신규)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   "우리는 게임이 아니라 유산을 만든다"                          │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   1. 전환(Switch)하지 말고, 심화(Deepen)시켜라                 │
│      ❌ 전체 테마 변경 (data-theme="heritage")                  │
│      ✅ Micro-elements 차별화 (점, 배지, 그림자)                │
│                                                                 │
│   2. 폭발(Explosion)이 아니라, 스며듦(Spread)이다              │
│      ❌ Golden Glow 폭발, 파티클 효과                           │
│      ✅ Ink Spread (0.6s, 묵직한 안착)                          │
│                                                                 │
│   3. 브랜드의 일관성(Consistency) 사수                          │
│      ❌ 새 컬러 도입 (Champagne Gold, Deep Charcoal)            │
│      ✅ 기존 Amber로 금빛 표현 (투명도, 그라디언트)             │
│                                                                 │
│   핵심: 명품은 소리치지 않는다. 디테일이 말한다.                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 컬러 시스템

### 2.1 Primary Colors

| 변수명 | 색상 | HEX | 용도 |
|--------|------|-----|------|
| `--dotting-deep-navy` | ![#1A365D](https://via.placeholder.com/20/1A365D/1A365D.png) | `#1A365D` | 메인 CTA, 헤더, 강조 텍스트 |
| `--dotting-warm-amber` | ![#F59E0B](https://via.placeholder.com/20/F59E0B/F59E0B.png) | `#F59E0B` | 성취/축하, 강조 포인트, 점 |
| `--dotting-soft-cream` | ![#FDF8F3](https://via.placeholder.com/20/FDF8F3/FDF8F3.png) | `#FDF8F3` | 배경 (Desk Color) |
| `--dotting-paper-white` | ![#FFFFFF](https://via.placeholder.com/20/FFFFFF/FFFFFF.png) | `#FFFFFF` | 책 속지 (Page Color) |
| `--dotting-warm-gray` | ![#F7F5F0](https://via.placeholder.com/20/F7F5F0/F7F5F0.png) | `#F7F5F0` | 섹션 구분용 |

### 2.2 Secondary Colors

| 변수명 | 색상 | HEX | 용도 |
|--------|------|-----|------|
| `--dotting-ocean-teal` | ![#0D9488](https://via.placeholder.com/20/0D9488/0D9488.png) | `#0D9488` | **기능적 성공 상태, 완료 체크** |
| `--dotting-rose-pink` | ![#F43F5E](https://via.placeholder.com/20/F43F5E/F43F5E.png) | `#F43F5E` | 감정적 하이라이트 |
| `--dotting-muted-gray` | ![#6B7280](https://via.placeholder.com/20/6B7280/6B7280.png) | `#6B7280` | 보조 텍스트 (중요 설명은 #4B5563) |

### 2.3 Functional Colors

| 변수명 | HEX | 용도 |
|--------|-----|------|
| `--dotting-warm-gold` | `#FEF3C7` | 사용자 말풍선 배경 |
| `--dotting-ai-bubble` | `#FFFBEB` | AI 말풍선 배경 |
| `--dotting-border` | `#E5E7EB` | 테두리 |
| `--dotting-card` | `#FFFFFF` | 카드 배경 |

### 2.4 컬러 사용 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   60-30-10 법칙                                                 │
│                                                                 │
│   60% - Soft Cream (#FDF8F3)                                    │
│         배경, 여백                                               │
│                                                                 │
│   30% - Paper White (#FFFFFF)                                   │
│         카드, 섹션, 모달                                         │
│                                                                 │
│   10% - Deep Navy + Warm Amber                                  │
│         CTA, 강조, 액센트                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.5 컬러 역할 명확화 (v1.1 추가)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   ⚠️ 중요: 컬러별 역할 분리                                     │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   🟠 Warm Amber (#F59E0B)                                       │
│   ✅ 사용: 점(●●●), 아이콘 강조, 브랜드 포인트                 │
│   ❌ 금지: 성취 피드백, CTA 버튼                                │
│   이유: 일반적으로 경고색으로 인식 가능                         │
│        → 브랜드 감성 전달에만 집중                              │
│                                                                 │
│   🔵 Ocean Teal (#0D9488)                                       │
│   ✅ 사용: 성공 상태, 완료 체크, 긍정 피드백                    │
│   이유: 기능적 성공의 시맨틱 컬러                               │
│                                                                 │
│   🔷 Deep Navy (#1A365D)                                        │
│   ✅ 사용: Primary CTA, 강조 텍스트, 헤더                       │
│   이유: 행동 유도의 명확한 신호                                 │
│                                                                 │
│   학습 효과:                                                    │
│   "주황색은 도팅의 색, 청록색은 성공의 신호"                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.5 컬러 조합 예시

```css
/* 히어로 섹션 */
background: var(--dotting-soft-cream);
color: var(--dotting-deep-navy);
accent: var(--dotting-warm-amber);

/* 카드 */
background: var(--dotting-paper-white);
border: 1px solid var(--dotting-border);
shadow: subtle

/* CTA 영역 */
background: var(--dotting-deep-navy);
color: white;
accent: var(--dotting-warm-amber);
```

---

## 3. 타이포그래피

### 3.1 폰트 패밀리

| 구분 | 폰트 | 변수 | 용도 |
|------|------|------|------|
| **Sans** | Pretendard Variable | `--font-sans` | 본문, UI 텍스트 |
| **Serif** | Noto Serif KR | `--font-serif` | 제목, 인용문, 책 느낌 |

### 3.2 폰트 스케일 (v1.1 단순화: 9단계 → 5단계)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   ⚠️ v1.1 핵심 변경: 타이포그래피 단순화                        │
│                                                                 │
│   이유:                                                         │
│   • 9단계는 과도하게 복잡 (일관성 저해)                         │
│   • 18px vs 20px 차이는 사용자가 인지 불가                      │
│   • 디자이너-개발자 커뮤니케이션 비용 증가                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

| 이름 | 크기 | Line Height | 용도 |
|------|------|-------------|------|
| **H1** | 36px | 1.2 | 히어로 제목 |
| **H2** | 24px | 1.35 | 섹션 제목 |
| **Body** | **17px** | 1.6 | 본문 (시니어 고려 상향) |
| **Caption** | 13px | 1.4 | 보조 설명 |
| **Label** | 12px | 1.2 | 라벨, 태그 |

**💡 Expert Tip:**
- **Body 17px**: 시니어 타겟에게 "돋보기 없는 편안함" 제공
- **Caption 색상**: 13px + Muted Gray는 시인성 저하 → `#4B5563` 사용 권장
- **행간 조정**: 17px에서 1.6 (약 27px)이 모바일에서 가장 단단함

### 3.3 행간 (Line Height)

| 변수 | 값 | 용도 |
|------|-----|------|
| `--line-height-body` | 1.7 | 본문 텍스트 |
| `--line-height-heading` | 1.3 | 제목 |
| `leading-relaxed` | 1.625 | 설명 텍스트 |

### 3.4 타이포그래피 조합 (v1.1 업데이트)

```html
<!-- 히어로 제목 (H1: 36px) -->
<h1 class="text-[36px] font-bold font-serif leading-[1.2] text-[var(--dotting-deep-navy)]">
  소중한 이야기를<br />
  <span class="relative inline-block">
    <span class="relative z-10">한 권의 책</span>
    <span class="absolute bottom-1 left-0 w-full h-3 -z-0 opacity-30 bg-[var(--dotting-warm-amber)]" />
  </span>
  으로
</h1>

<!-- 섹션 제목 (H2: 24px) -->
<h2 class="text-[24px] font-bold font-serif leading-[1.35] text-[var(--dotting-deep-navy)] mb-6">
  이렇게 만들어집니다
</h2>

<!-- 본문 (Body: 17px) -->
<p class="text-[17px] text-[var(--dotting-muted-gray)] leading-[1.6]">
  이야기만 들려주세요. 아름다운 책으로 엮어드릴게요.
</p>

<!-- 캡션 (Caption: 13px) -->
<p class="text-[13px] text-[#4B5563] leading-[1.4]">
  *샘플 기준, 실제 분량은 대화에 따라 달라집니다
</p>

<!-- 라벨 (Label: 12px) -->
<p class="text-[12px] font-medium tracking-widest text-[var(--dotting-warm-amber)] mb-4">
  PROCESS
</p>
```

**💡 Tailwind Config 설정:**

```js
// tailwind.config.js
fontSize: {
  'label': ['12px', { lineHeight: '1.2' }],
  'caption': ['13px', { lineHeight: '1.4' }],
  'body': ['17px', { lineHeight: '1.6' }],    // 핵심: 상향
  'h2': ['24px', { lineHeight: '1.35' }],
  'h1': ['36px', { lineHeight: '1.2' }],
}
```

---

## 4. 시각적 아이덴티티

### 4.1 ●●● 점 시스템 (Signature Dots) - v1.2 패키지 정체성

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   ⚠️ v1.2 핵심 변경: 진행 상태 → 패키지 정체성                  │
│                                                                 │
│   v1.1: 진행 상태바 (인터랙티브)                                │
│   v1.2: 상품 등급 시각화 (PackageType Enum)                    │
│                                                                 │
│   이유: PRD v4.0 "Prologue-Essay-Story-Heritage" 구조           │
│         → 점이 곧 패키지 등급을 정의                            │
│                                                                 │
│   "점을 이어 선으로, 선을 모아 이야기로"                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 패키지별 점 표현 (PackageType Enum)

| 패키지 | 점 | 가격 | 의미 |
|--------|-----|------|------|
| **Prologue** | ○ ○ ○ | 0원 | 비어있음 - 이야기의 시작 |
| **Essay** | ● ○ ○ | 3.9만원 | 1개 채워짐 - 첫 점이 찍힘 |
| **Story** | ● ● ○ | 9.9만원 | 2개 채워짐 - 점이 선으로 이어짐 |
| **Heritage** | ● ● ● | 19.9만원 | 모두 채워짐 - 완전한 유산 |

**TypeScript 정의:**
```typescript
type PackageType = 'prologue' | 'essay' | 'story' | 'heritage';

interface DotConfig {
  package: PackageType;
  filled: [boolean, boolean, boolean]; // [첫번째, 두번째, 세번째]
  variant: 'default' | 'heritage';     // Heritage는 금박 효과
}

// 예시
const essayDots: DotConfig = {
  package: 'essay',
  filled: [true, false, false],
  variant: 'default'
};

const heritageDots: DotConfig = {
  package: 'heritage',
  filled: [true, true, true],
  variant: 'heritage'  // 금박 그라디언트
};
```

#### Ink Spread 애니메이션 (v1.2 신규)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   "폭발이 아니라, 스며듦"                                       │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   타이밍: 0.6s (묵직한 안착감)                                  │
│   Easing: cubic-bezier(0.25, 0.1, 0.25, 1)                      │
│                                                                 │
│   단계:                                                         │
│   0%:   투명 (opacity: 0, scale: 0.6)                           │
│   40%:  중심 채워짐 (opacity: 0.8, scale: 0.95)                 │
│   80%:  가장자리 스며듦 (opacity: 1, scale: 1.02)               │
│   100%: 안착 (opacity: 1, scale: 1)                             │
│                                                                 │
│   완료 후: Breathing Glow                                       │
│   - opacity: 0.3 (매우 은은)                                    │
│   - animation: 2s infinite                                      │
│   - 범위: 점 주변 2px만                                         │
│                                                                 │
│   ❌ 금지: Golden Glow 폭발, 파티클 효과                        │
│   ✅ 원칙: 종이에 잉크가 스며드는 물리적 속도감                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**CSS 구현:**
```css
@keyframes dotting-ink-spread {
  0% {
    opacity: 0;
    transform: scale(0.6) translateZ(0);
  }
  40% {
    opacity: 0.8;
    transform: scale(0.95) translateZ(0);
  }
  80% {
    opacity: 1;
    transform: scale(1.02) translateZ(0);
  }
  100% {
    opacity: 1;
    transform: scale(1) translateZ(0);
  }
}

@keyframes breathing-glow {
  0%, 100% { opacity: 0.25; }
  50% { opacity: 0.35; }
}

.dot.is-filling::after {
  animation: dotting-ink-spread 0.6s cubic-bezier(0.25, 0.1, 0.25, 1);
}

.dot.filled::after {
  animation: breathing-glow 2s ease-in-out infinite;
}
```

#### Heritage 금박 효과 (v1.2 신규)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   "기존 Amber로 금빛 표현" (새 컬러 코드 0개)                   │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   레이어 1: 그라디언트 (명도 조절)                              │
│   linear-gradient(135deg,                                       │
│     #F59E0B 0%,      /* 기존 Amber */                           │
│     #FCD34D 50%,     /* 밝은 Amber */                           │
│     #F59E0B 100%                                                │
│   )                                                             │
│                                                                 │
│   레이어 2: SVG 노이즈 (종이 질감)                              │
│   - fractalNoise (opacity: 0.1)                                 │
│   - overlay blend mode                                          │
│                                                                 │
│   레이어 3: inset 그림자 (입체감)                               │
│   box-shadow:                                                   │
│     inset 0 1px 1px rgba(252, 211, 77, 0.8),                    │
│     inset 0 -1px 1px rgba(180, 83, 9, 0.3);                     │
│                                                                 │
│   애니메이션: Gold Shimmer (1.2s)                               │
│   - background-position 이동                                    │
│   - 은은한 반짝임 (과하지 않게)                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**CSS 구현:**
```css
.dot-heritage {
  background: linear-gradient(
    135deg,
    #F59E0B 0%,
    #FCD34D 50%,
    #F59E0B 100%
  );
  background-size: 200% 200%;
  
  /* 종이 질감 */
  background-image: 
    linear-gradient(135deg, #F59E0B 0%, #FCD34D 50%, #F59E0B 100%),
    url("data:image/svg+xml,..."); /* SVG 노이즈 */
  background-blend-mode: overlay;
  
  /* 입체감 */
  box-shadow: 
    inset 0 1px 1px rgba(252, 211, 77, 0.8),
    inset 0 -1px 1px rgba(180, 83, 9, 0.3),
    0 2px 4px rgba(245, 158, 11, 0.3);
}

.dot-heritage.is-filling {
  animation: 
    dotting-ink-spread 0.8s cubic-bezier(0.2, 0.05, 0.2, 1),
    gold-shimmer 1.2s ease-in-out;
}

@keyframes gold-shimmer {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}
```

### 4.2 Heritage 차별화 전략 (v1.2 신규)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   "전환하지 말고, 심화시켜라" (Subtle Distinction)              │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   ❌ 거부: data-theme="heritage" 전체 테마 전환                 │
│   ✅ 원칙: Micro-elements만 차별화 (5% 미만 변경)               │
│                                                                 │
│   변경 요소:                                                    │
│   1. 점(Dot) 텍스처 - 금박 그라디언트                           │
│   2. 프로필 배지 - "VIP" 작은 배지 (상단 우측)                  │
│   3. 버튼 그림자 - Navy → Amber 힌트                            │
│   4. 완료 체크마크 - Teal → Amber 그라디언트                    │
│                                                                 │
│   불변 요소:                                                    │
│   • 배경 (Soft Cream)                                           │
│   • 텍스트 (Deep Navy)                                          │
│   • 레이아웃                                                    │
│   • 타이포그래피                                                │
│                                                                 │
│   핵심: "나는 특별하다"는 느낌 + 혼란 없음                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### Heritage 차별화 상세

| 요소 | 기본 사용자 | Heritage 사용자 | 변경 범위 |
|------|------------|----------------|----------|
| **배경** | Soft Cream | Soft Cream | 변경 없음 |
| **텍스트** | Deep Navy | Deep Navy | 변경 없음 |
| **점** | 단색 Amber | 그라디언트 Amber + Shimmer | 점만 |
| **배지** | 없음 | "VIP" 금박 테두리 (16px) | 상단 우측만 |
| **버튼 그림자** | `rgba(26, 54, 93, 0.1)` | `rgba(245, 158, 11, 0.08)` | 그림자 색만 |
| **체크마크** | Teal | Amber 그라디언트 | 아이콘만 |
| **로딩 스피너** | Navy | Amber 그라디언트 | 스피너만 |

**CSS 변수 전략:**
```css
/* 기본 사용자 (변경 없음) */
:root {
  --dotting-warm-amber: #F59E0B;
  --dotting-deep-navy: #1A365D;
  --shadow-accent: rgba(26, 54, 93, 0.1);
}

/* Heritage 사용자 (body에 .heritage 클래스 추가) */
.heritage {
  --shadow-accent: rgba(245, 158, 11, 0.08);
  --dot-gradient: linear-gradient(135deg, #F59E0B, #FCD34D, #F59E0B);
  --check-color: var(--dot-gradient);
}

/* 적용 예시 */
.button {
  box-shadow: 0 2px 4px var(--shadow-accent);
}

.heritage .dot {
  background: var(--dot-gradient);
}
```

**VIP 배지 구현:**
```html
<!-- Heritage 사용자만 표시 -->
<div class="heritage">
  <div class="fixed top-4 right-4 z-50">
    <span class="inline-flex items-center gap-1 px-2 py-1 text-[10px] font-bold tracking-widest text-[var(--dotting-warm-amber)] border border-[var(--dotting-warm-amber)] rounded-full">
      VIP
    </span>
  </div>
</div>
```

### 4.3 점 구현 코드 (v1.2 업데이트)

```tsx
// src/components/ui/dot.tsx
interface DotProps {
  package: 'prologue' | 'essay' | 'story' | 'heritage';
  animated?: boolean;
  onFillComplete?: () => void;
}

export function Dot({ package: pkg, animated, onFillComplete }: DotProps) {
  const config = {
    prologue: { filled: [false, false, false], variant: 'default' },
    essay: { filled: [true, false, false], variant: 'default' },
    story: { filled: [true, true, false], variant: 'default' },
    heritage: { filled: [true, true, true], variant: 'heritage' },
  }[pkg];

  return (
    <div className="flex items-center gap-1">
      {config.filled.map((isFilled, index) => (
        <span
          key={index}
          className={cn(
            'w-1.5 h-1.5 rounded-full',
            isFilled 
              ? config.variant === 'heritage'
                ? 'dot-heritage'
                : 'bg-[var(--dotting-warm-amber)]'
              : 'bg-[var(--dotting-warm-amber)]/30',
            animated && isFilled && 'is-filling'
          )}
        />
      ))}
    </div>
  );
}

// 사용 예시
<Dot package="essay" />
<Dot package="heritage" animated onFillComplete={() => haptics.light()} />
```

### 4.4 오디오 UI 시스템 (v1.2 신규)

**유니버설 디자인**: 시니어와 젊은 세대 모두를 위한 음성 메시지 UI

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   "텍스트 중심 → 목소리 중심 멀티모달"                          │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   PRD v4.0 핵심: 오디오 QR, 목소리 보존                         │
│   → 채팅 UI는 텍스트만이 아닌 오디오 우선                       │
│   → 보물 마킹: 30초 티저 클립 후보 선택                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### Message 타입 확장 (✅ 구현 완료)

```typescript
// types/database.ts
export interface MessageMetadata {
  // 보물 (Treasure) 메타데이터
  is_treasure?: boolean
  treasure_index?: number // 선택된 순서 (1부터)
  marked_at?: string // ISO 8601
  
  // 오디오 메타데이터
  duration?: number // 초
  waveform?: number[] // 0~1 사이 값 (최대 50개)
}

export type UniversalMessage = Message & {
  meta?: MessageMetadata
}
```

#### UI 상태 분기

| 메시지 타입 | UI 표현 | 컬러 | 인터랙션 |
|------------|---------|------|----------|
| **텍스트 답변** | 기본 말풍선 | User: Deep Navy<br>AI: Soft Cream | 맥락적 그룹핑 |
| **오디오 답변** | 파형 + 재생 버튼 | Amber 파형 | 재생/일시정지 |
| **녹음 중** | 실시간 파형 + 타이머 | Amber 펄스 | 중지 버튼 |
| **처리 중** | 로딩 스피너 | Teal | - |

#### 오디오 말풍선 구현 (✅ 구현 완료)

```tsx
// components/audio/AudioMessage.tsx
import { AudioMessage } from '@/components/audio/AudioMessage'

<AudioMessage
  message={message} // UniversalMessage 타입
  variant="heritage" // 'default' | 'heritage'
  onTreasureMark={async (id, isTreasure) => {
    // 보물 마킹 로직
    await updateMessageMetadata(id, { is_treasure: isTreasure })
  }}
/>
```

**Lead PM 가이드 반영**:
- ✅ 별 아이콘 + "보물 1" 간결한 조합 (Label 12px)
- ✅ Heritage Glow 0.6초, 투명도 0.35 이하
- ✅ 햅틱(10ms) + 토스트 애니메이션 동기화
- ✅ GPU 최적화 (`will-change: opacity`)

#### Heritage Glow Animation (✅ 구현 완료)

```css
/* globals.css */
.audio-waveform-bar.heritage.active::after {
  content: '';
  position: absolute;
  inset: -2px;
  background: radial-gradient(
    circle, 
    rgba(245, 158, 11, 0.35) 0%, 
    transparent 70%
  );
  animation: heritage-glow 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  will-change: opacity;
}

@keyframes heritage-glow {
  0% { opacity: 0; transform: scale(0.8); }
  50% { opacity: 1; }
  100% { opacity: 0.35; transform: scale(1); }
}
```

#### Haptic Feedback (✅ 구현 완료)

```typescript
// lib/haptic.ts
export function triggerTreasureFeedback(element: HTMLElement | null): void {
  // 햅틱: 10ms (light)
  triggerHaptic('light')
  
  // 시각: Amber glow
  element?.classList.add('treasure-glow')
  setTimeout(() => element?.classList.remove('treasure-glow'), 200)
  const [currentTime, setCurrentTime] = useState(0);

  return (
    <div className="relative flex items-center gap-3 p-4 rounded-2xl bg-[var(--dotting-paper-white)] border border-[var(--dotting-border)]">
      {/* 재생 버튼 */}
      <button
        onClick={() => setIsPlaying(!isPlaying)}
        className="flex-shrink-0 w-10 h-10 rounded-full bg-[var(--dotting-warm-amber)] text-white flex items-center justify-center"
      >
        {isPlaying ? <PauseIcon /> : <PlayIcon />}
      </button>

      {/* 파형 시각화 */}
      <div className="flex-1 flex items-center gap-0.5 h-8">
        {waveform?.map((height, index) => (
          <div
            key={index}
            className="flex-1 bg-[var(--dotting-warm-amber)] rounded-full transition-all"
            style={{
              height: `${height * 100}%`,
              opacity: (index / waveform.length) <= (currentTime / duration) ? 1 : 0.3
            }}
          />
        ))}
      </div>

      {/* 시간 표시 */}
      <span className="text-[13px] text-[var(--dotting-muted-gray)] font-mono">
        {formatTime(currentTime)} / {formatTime(duration)}
      </span>

      {/* 티저 마킹 버튼 */}
      <button
        onClick={onToggleHighlight}
        className={cn(
          "absolute top-2 right-2 w-6 h-6 flex items-center justify-center rounded-full transition-colors",
          isHighlight 
            ? "text-[var(--dotting-warm-amber)]" 
            : "text-[var(--dotting-muted-gray)] hover:text-[var(--dotting-warm-amber)]"
        )}
      >
        <StarIcon className={isHighlight ? "fill-current" : ""} />
      </button>
    </div>
  );
}
```

#### 녹음 중 UI

```tsx
// 녹음 중 상태
export function RecordingIndicator({ duration }: { duration: number }) {
  return (
    <div className="flex items-center gap-3 p-4 rounded-2xl bg-[var(--dotting-warm-gold)] border-2 border-[var(--dotting-warm-amber)]">
      {/* 펄스 애니메이션 */}
      <div className="relative w-10 h-10">
        <div className="absolute inset-0 rounded-full bg-[var(--dotting-warm-amber)] animate-ping opacity-75" />
        <div className="relative w-10 h-10 rounded-full bg-[var(--dotting-warm-amber)] flex items-center justify-center">
          <MicIcon className="w-5 h-5 text-white" />
        </div>
      </div>

      {/* 실시간 파형 (라이브) */}
      <div className="flex-1 flex items-center gap-0.5 h-8">
        {Array.from({ length: 40 }).map((_, index) => (
          <div
            key={index}
            className="flex-1 bg-[var(--dotting-warm-amber)] rounded-full animate-pulse"
            style={{
              height: `${Math.random() * 100}%`,
              animationDelay: `${index * 0.05}s`
            }}
          />
        ))}
      </div>

      {/* 타이머 */}
      <span className="text-[17px] font-mono text-[var(--dotting-deep-navy)]">
        {formatTime(duration)}
      </span>
    </div>
  );
}
```

#### 티저 마킹 토스트

```tsx
// 티저 선택 시 토스트 메시지
<Toast>
  <div className="flex items-center gap-2">
    <StarIcon className="w-5 h-5 text-[var(--dotting-warm-amber)]" />
    <span className="text-[15px] text-[var(--dotting-deep-navy)]">
      이 이야기는 30초 티저로 제작됩니다
    </span>
  </div>
</Toast>

// 햅틱 피드백 (모바일)
if ('vibrate' in navigator) {
  navigator.vibrate(10);
}
```

#### 성공 피드백 (Teal 사용)

```tsx
// 녹음 완료 시
<div className="flex items-center gap-2 p-3 rounded-xl bg-[var(--dotting-ocean-teal)]/10 border border-[var(--dotting-ocean-teal)]">
  <CheckCircleIcon className="w-5 h-5 text-[var(--dotting-ocean-teal)]" />
  <span className="text-[15px] text-[var(--dotting-ocean-teal)] font-medium">
    녹음이 완료되었습니다
  </span>
</div>
```

### 4.5 점 사이즈 변형

| 변수 | 크기 | 용도 |
|------|------|------|
| `--dot-size-sm` | 4px | 작은 인라인 |
| `--dot-size-md` | 6px | 기본 |
| `--dot-size-lg` | 8px | 강조 (CTA 영역) |

### 4.4 워드마크

```html
<!-- 기본 워드마크 -->
<div class="dotting-wordmark dotting-wordmark--lg">
  <span class="dotting-wordmark-d">D</span>
  <span class="dotting-wordmark-otting">OTTING</span>
</div>

<!-- 흰색 버전 (푸터) -->
<div class="dotting-wordmark dotting-wordmark--md dotting-wordmark--white">
  <span class="dotting-wordmark-d">D</span>
  <span class="dotting-wordmark-otting">OTTING</span>
</div>
```

| 클래스 | 크기 |
|--------|------|
| `dotting-wordmark--sm` | 16px |
| `dotting-wordmark--md` | 20px |
| `dotting-wordmark--lg` | 24px |
| `dotting-wordmark--xl` | 32px |

---

## 5. 컴포넌트

### 5.1 버튼 (Button) - v1.1 단순화

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   ⚠️ v1.1 핵심 변경: 7개 → 4개 Variants                         │
│                                                                 │
│   제거: secondary, outline, link                                │
│   이유: 위계 모호, 화면 지저분                                  │
│                                                                 │
│   유지: Primary vs Ghost 이분법                                 │
│   → "사용자가 생각하게 하지 마라"                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### Variants (v1.1)

| Variant | 용도 | 예시 | 색상 |
|---------|------|------|------|
| `default` | **Primary CTA** (화면당 1개) | 지금 시작하기 | Deep Navy |
| `ghost` | 비강조 액션 | 도팅 스토리, 취소 | Muted Gray |
| `celebration` | 성취 모먼트만 | 책 완성! | Amber→Orange |
| `destructive` | 삭제/경고만 | 삭제하기 | Red |

#### Sizes

| Size | 높이 | 용도 |
|------|------|------|
| `sm` | 32px | 네비게이션, 소형 |
| `default` | 40px | 일반 |
| `lg` | 48px | 카드 내 CTA |
| `xl` | 56px | 히어로 CTA |

#### 사용 예시

```tsx
import { Button } from '@/components/ui/button';

// Primary CTA
<Button size="xl">지금 시작하기</Button>

// Secondary
<Button variant="secondary" size="xl">샘플 먼저 볼게요</Button>

// Ghost (Navigation)
<Button variant="ghost" size="sm">도팅 스토리</Button>

// Celebration
<Button variant="celebration" size="lg">지금 시작하기</Button>

// Outline
<Button variant="outline" size="lg" className="w-full">시작하기</Button>
```

#### 버튼 계층 원칙 (v1.1 강화)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   화면당 Primary CTA는 1개만                                    │
│                                                                 │
│   ─────────────────────────────────                             │
│                                                                 │
│   ✅ 좋은 예 (v1.1)                                             │
│   • [지금 시작하기] ← Primary (Deep Navy)                       │
│   • [샘플 먼저 볼게요] ← Ghost (텍스트 링크처럼)                │
│                                                                 │
│   ❌ 나쁜 예                                                    │
│   • [지금 시작하기] [가입하기] [둘러보기] ← Primary 3개         │
│   • [시작하기] [샘플 보기] ← Secondary 2개 (혼란)               │
│                                                                 │
│   💡 Expert Tip:                                                │
│   Ghost 버튼은 Primary 하단 또는 좌측 배치                      │
│   → 시선이 Primary에서 끝나도록                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 카드 (Card)

```tsx
// 기본 카드
<div className="bg-white rounded-3xl p-8 border border-[var(--dotting-border)] shadow-lg">
  {/* content */}
</div>

// 강조 카드 (추천)
<div className="bg-white rounded-3xl p-8 border-2 border-[var(--dotting-warm-amber)] shadow-2xl transform md:scale-105">
  <div className="absolute top-0 right-0 bg-[var(--dotting-warm-amber)] text-white text-xs font-bold px-4 py-2 rounded-bl-xl">
    EARLY BIRD
  </div>
  {/* content */}
</div>

// 신뢰 요소 카드
<div className="text-center p-6 bg-[var(--dotting-soft-cream)] rounded-2xl">
  <div className="w-12 h-12 rounded-full bg-white flex items-center justify-center mx-auto mb-4 text-[var(--dotting-warm-amber)]">
    {/* icon */}
  </div>
  {/* content */}
</div>
```

### 5.3 인풋 (Input)

```tsx
// 기본 인풋
<input
  type="text"
  className="w-full px-4 py-3 rounded-xl border border-[var(--dotting-border)] 
             focus:border-[var(--dotting-deep-navy)] focus:ring-2 focus:ring-[var(--dotting-warm-amber)]/20
             outline-none transition-all"
  placeholder="이메일을 입력하세요"
/>

// 텍스트 영역
<textarea
  className="w-full px-4 py-3 rounded-xl border border-[var(--dotting-border)]
             focus:border-[var(--dotting-deep-navy)] focus:ring-2 focus:ring-[var(--dotting-warm-amber)]/20
             outline-none transition-all resize-none"
  rows={4}
  placeholder="이야기를 들려주세요..."
/>
```

### 5.4 말풍선 (Chat Bubble) - v1.1 대비 강화

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   ⚠️ v1.1 변경: 명도 대비 강화                                  │
│                                                                 │
│   문제: Warm Gold (#FEF3C7) vs AI Bubble (#FFFBEB)             │
│         → 명도 차이 작음, 야외/저전력 모드에서 구분 어려움      │
│                                                                 │
│   해결: 사용자 말풍선 Deep Navy 배경 (강한 대비)                │
│         AI 말풍선 테두리 추가 (시인성 확보)                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```tsx
// AI 말풍선 (v1.1: 테두리 추가)
<div className="px-4 py-3 max-w-[80%] bg-[var(--dotting-ai-bubble)] border border-[var(--dotting-border)] rounded-2xl rounded-tl-sm">
  <p className="text-[17px] text-[#1F2937] leading-[1.6]">
    어린 시절 가장 기억에 남는 장소가 있으신가요?
  </p>
</div>

// 사용자 말풍선 (v1.1: Deep Navy 배경)
<div className="px-4 py-3 max-w-[80%] ml-auto bg-[var(--dotting-deep-navy)] rounded-2xl rounded-tr-sm">
  <p className="text-[17px] text-white leading-[1.6]">
    부산 해운대에서 자랐어요...
  </p>
</div>
```

**CSS 변수 업데이트:**

```css
/* v1.1: AI 말풍선 개선 */
.dotting-bubble-ai {
  background-color: var(--dotting-ai-bubble);
  border: 1px solid #E5E7EB; /* 테두리 추가 */
  color: #1F2937; /* 글자색 진하게 */
  border-radius: 1rem;
  border-top-left-radius: 0.25rem;
}

/* v1.1: 사용자 말풍선 대비 강화 */
.dotting-bubble-user {
  background-color: var(--dotting-deep-navy); /* Deep Navy */
  color: white; /* 흰색 텍스트 */
  border-radius: 1rem;
  border-top-right-radius: 0.25rem;
}
```

### 5.5 체크리스트 아이템

```tsx
<div className="flex items-center gap-2">
  <span className="text-[var(--dotting-warm-amber)]">✓</span>
  <span className="text-[var(--dotting-muted-gray)]">PDF 다운로드</span>
</div>

// 또는
<li className="flex items-start gap-2 text-[var(--dotting-deep-navy)]">
  <span className="text-[var(--dotting-warm-amber)] mt-0.5">✓</span>
  <span>프리미엄 하드커버 1권</span>
</li>
```

### 5.6 FAQ 아코디언

```tsx
<details className="group bg-white rounded-xl border border-[var(--dotting-border)]">
  <summary className="px-6 py-4 cursor-pointer flex items-center justify-between font-medium text-[var(--dotting-deep-navy)] hover:bg-gray-50 rounded-xl">
    결제는 언제 하나요?
    <span className="text-[var(--dotting-warm-amber)] group-open:rotate-180 transition-transform">▾</span>
  </summary>
  <p className="px-6 pb-4 text-sm text-[var(--dotting-muted-gray)] leading-relaxed">
    패키지 선택 후 먼저 결제해주시면, 인터뷰 링크가 생성됩니다.
  </p>
</details>
```

---

## 6. 레이아웃

### 6.1 그리드 시스템

| 변수 | 값 | 용도 |
|------|-----|------|
| `--content-max-width` | 720px | 본문 최대 폭 |
| `max-w-6xl` | 1152px | 섹션 최대 폭 |
| `max-w-4xl` | 896px | 좁은 섹션 |
| `max-w-2xl` | 672px | FAQ 등 |

### 6.2 섹션 구조

```tsx
// 표준 섹션
<section className="py-24 lg:py-28 px-6 bg-white">
  <div className="max-w-6xl mx-auto">
    {/* 헤더 */}
    <div className="text-center mb-16">
      <p className="text-sm font-medium tracking-widest text-[var(--dotting-warm-amber)] mb-4">
        LABEL
      </p>
      <h2 className="text-3xl md:text-4xl font-bold font-serif text-[var(--dotting-deep-navy)] mb-6">
        섹션 제목
      </h2>
      <p className="text-[var(--dotting-muted-gray)] max-w-md mx-auto">
        설명 텍스트
      </p>
    </div>
    {/* 콘텐츠 */}
  </div>
</section>

// 배경 교차
<section className="py-24 px-6 bg-[var(--dotting-soft-cream)]">...</section>
<section className="py-24 px-6 bg-white">...</section>
<section className="py-24 px-6 bg-[var(--dotting-warm-gray)]">...</section>
```

### 6.3 간격 시스템

| 클래스 | 용도 |
|--------|------|
| `py-12` | 작은 섹션 |
| `py-20` | 중간 섹션 |
| `py-24 lg:py-28` | 표준 섹션 |
| `mb-4` | 라벨과 제목 사이 |
| `mb-6` | 제목과 설명 사이 |
| `mb-8` | 설명과 버튼 사이 |
| `mb-16` | 헤더와 콘텐츠 사이 |
| `gap-6 lg:gap-8` | 카드 그리드 간격 |

### 6.4 네비게이션

```tsx
<nav className="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-md border-b border-[var(--dotting-border)]">
  <div className="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
    {/* 로고 */}
    <Link href="/" className="dotting-wordmark dotting-wordmark--lg">
      <span className="dotting-wordmark-d">D</span>
      <span className="dotting-wordmark-otting">OTTING</span>
    </Link>

    {/* 네비게이션 */}
    <div className="flex items-center gap-4">
      <Button variant="ghost" size="sm">도팅 스토리</Button>
      <Button size="sm">도팅 로그인</Button>
    </div>
  </div>
</nav>
```

### 6.5 푸터

```tsx
<footer className="py-12 px-6 bg-[#0F1F2E]">
  <div className="max-w-5xl mx-auto">
    <div className="flex flex-col md:flex-row items-center justify-between gap-6">
      {/* 로고 (흰색) */}
      <div className="dotting-wordmark dotting-wordmark--md dotting-wordmark--white">...</div>
      
      {/* 링크 */}
      <div className="flex gap-6 text-sm text-white/50">
        <Link href="/why" className="hover:text-white transition-colors">도팅 스토리</Link>
        <Link href="/terms" className="hover:text-white transition-colors">이용약관</Link>
        <Link href="/privacy" className="hover:text-white transition-colors">개인정보처리방침</Link>
      </div>
    </div>

    <div className="mt-8 pt-8 text-center border-t border-white/10">
      <p className="text-xs text-white/30">© 2026 DOTTING. All rights reserved.</p>
    </div>
  </div>
</footer>
```

---

## 7. 애니메이션

### 7.1 기본 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   애니메이션 원칙                                               │
│                                                                 │
│   • 자연스럽고 부드럽게 (ease-out)                              │
│   • 0.2~0.6초 내 완료                                           │
│   • 의미 있는 움직임만                                          │
│   • 과도한 효과 지양                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 페이드 인 업 (Hero)

```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(24px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in-up {
  animation: fadeInUp 0.6s ease-out forwards;
}
```

### 7.3 스크롤 리빌 (Intersection Observer)

```css
.reveal {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity 0.6s ease-out, transform 0.6s ease-out;
}

.reveal.revealed {
  opacity: 1;
  transform: translateY(0);
}
```

```tsx
// React Hook
function useScrollReveal() {
  useEffect(() => {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('revealed');
          }
        });
      },
      { threshold: 0.1, rootMargin: '0px 0px -50px 0px' }
    );

    document.querySelectorAll('.reveal').forEach((el) => observer.observe(el));
    return () => observer.disconnect();
  }, []);
}
```

### 7.4 로딩 점 애니메이션

```css
.dotting-dots--loading .dotting-dot {
  animation: dotting-pulse 1.5s ease-in-out infinite;
}

.dotting-dots--loading .dotting-dot:nth-child(1) { animation-delay: 0s; }
.dotting-dots--loading .dotting-dot:nth-child(2) { animation-delay: 0.3s; }
.dotting-dots--loading .dotting-dot:nth-child(3) { animation-delay: 0.6s; }

@keyframes dotting-pulse {
  0%, 100% {
    background-color: var(--dot-inactive);
    transform: scale(1);
  }
  50% {
    background-color: var(--dot-active);
    transform: scale(1.2);
  }
}
```

### 7.5 버튼 인터랙션

```css
/* Hover */
hover:-translate-y-0.5
hover:shadow-md

/* Active */
active:scale-[0.98]

/* Transition */
transition-all duration-200
```

---

## 8. 아이콘 & 이미지

### 8.1 아이콘 스타일

```tsx
// Heroicons (Outline, strokeWidth 1.5)
<svg className="w-6 h-6" fill="none" stroke="currentColor" strokeWidth={1.5} viewBox="0 0 24 24">
  <path strokeLinecap="round" strokeLinejoin="round" d="..." />
</svg>

// 아이콘 컨테이너
<div className="w-12 h-12 rounded-full bg-white flex items-center justify-center text-[var(--dotting-warm-amber)]">
  {/* icon */}
</div>
```

### 8.2 이미지 처리

```tsx
// 히어로 이미지
<div className="relative overflow-hidden rounded-2xl aspect-[4/5] shadow-2xl">
  <Image
    src="/images/hero-book.jpg"
    alt="소중한 이야기가 담긴 책"
    fill
    priority
    sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 480px"
    className="object-cover object-[center_55%]"
  />
  {/* 그라데이션 오버레이 */}
  <div className="absolute inset-0 bg-gradient-to-t from-[var(--dotting-deep-navy)] via-[var(--dotting-deep-navy)]/40 to-transparent" />
</div>
```

### 8.3 책 목업

```tsx
// 3D 회전 책 표지
<div style={{ perspective: '800px' }}>
  <div
    className="w-48 h-64 rounded-r-md shadow-xl"
    style={{
      transform: 'rotateY(-8deg)',
      background: 'linear-gradient(145deg, #1E3A5F 0%, #1E3A5Fdd 100%)',
    }}
  >
    {/* 책 내용 */}
  </div>
</div>
```

---

## 9. 반응형 디자인

### 9.1 브레이크포인트

| 이름 | 크기 | Tailwind |
|------|------|----------|
| Mobile | < 640px | (기본) |
| Tablet | ≥ 640px | `sm:` |
| Desktop SM | ≥ 768px | `md:` |
| Desktop | ≥ 1024px | `lg:` |
| Wide | ≥ 1280px | `xl:` |

### 9.2 반응형 패턴

```tsx
// 그리드
<div className="grid md:grid-cols-3 gap-6 lg:gap-8">

// 히어로 레이아웃
<div className="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">

// 텍스트 크기
<h1 className="text-4xl md:text-5xl lg:text-[3.25rem]">

// 패딩
<section className="py-24 lg:py-28 px-6">

// Flex 방향
<div className="flex flex-col sm:flex-row gap-4">
```

### 9.3 모바일 우선 원칙

```
✅ 모바일 먼저 디자인하고 확대
   text-4xl → md:text-5xl → lg:text-[3.25rem]

❌ 데스크톱 먼저 디자인하고 축소
```

---

## 10. 접근성

### 10.1 포커스 스타일

```css
/* 모든 인터랙티브 요소 */
focus-visible:ring-2 
focus-visible:ring-offset-2 
focus-visible:ring-[var(--dotting-warm-amber)]
```

### 10.2 색상 대비

| 조합 | 비율 | 상태 |
|------|------|------|
| Deep Navy on Soft Cream | 7.5:1 | ✅ AAA |
| Muted Gray on White | 4.6:1 | ✅ AA |
| White on Deep Navy | 8.5:1 | ✅ AAA |

### 10.3 시맨틱 HTML

```tsx
// 헤딩 계층
<h1>  // 페이지당 1개
<h2>  // 섹션 제목
<h3>  // 서브 섹션

// 랜드마크
<nav>     // 네비게이션
<main>    // 주요 콘텐츠
<section> // 섹션
<footer>  // 푸터

// ARIA
<button aria-label="메뉴 열기">
<img alt="소중한 이야기가 담긴 책">
```

### 10.4 키보드 네비게이션

```
Tab         → 다음 요소로 이동
Shift+Tab   → 이전 요소로 이동
Enter/Space → 버튼 활성화
Escape      → 모달 닫기
```

---

## 부록: 빠른 참조

### A. CSS 변수 전체 목록

```css
:root {
  /* Primary */
  --dotting-deep-navy: #1A365D;
  --dotting-warm-amber: #F59E0B;
  --dotting-soft-cream: #FDF8F3;
  --dotting-paper-white: #FFFFFF;
  --dotting-warm-gray: #F7F5F0;
  
  /* Secondary */
  --dotting-ocean-teal: #0D9488;
  --dotting-rose-pink: #F43F5E;
  --dotting-muted-gray: #6B7280;
  
  /* Functional */
  --dotting-warm-gold: #FEF3C7;
  --dotting-ai-bubble: #FFFBEB;
  --dotting-border: #E5E7EB;
  --dotting-card: #FFFFFF;
  
  /* Typography */
  --font-sans: 'Pretendard Variable', ...;
  --font-serif: 'Noto Serif KR', ...;
  --line-height-body: 1.7;
  --line-height-heading: 1.3;
  
  /* Layout */
  --content-max-width: 720px;
  
  /* Dots */
  --dot-size-sm: 4px;
  --dot-size-md: 6px;
  --dot-size-lg: 8px;
  --dot-active: var(--dotting-warm-amber);
  --dot-inactive: #E5E7EB;
}
```

### B. 유틸리티 클래스

| 클래스 | 용도 |
|--------|------|
| `.dotting-prose` | 본문 폭 제한 |
| `.dotting-serif` | 책 느낌 타이포 |
| `.dotting-bubble-ai` | AI 말풍선 |
| `.dotting-bubble-user` | 사용자 말풍선 |
| `.dotting-btn-primary` | Primary 버튼 |
| `.dotting-btn-secondary` | Secondary 버튼 |
| `.dotting-btn-ghost` | Ghost 버튼 |
| `.dotting-dots` | 점 컨테이너 |
| `.dotting-wordmark` | 워드마크 |
| `.reveal` | 스크롤 애니메이션 |

### C. 체크리스트

```
새 페이지 생성 시:
□ Primary CTA 1개만 있는지 확인
□ 섹션별 배경색 교차 확인
□ reveal 클래스 적용 확인
□ 반응형 테스트 완료
□ 접근성 검사 완료

새 컴포넌트 생성 시:
□ CSS 변수 사용 확인
□ 호버/포커스 상태 정의
□ 반응형 처리 완료
□ 키보드 접근성 확인
```

---

## 부록 B: v1.1 즉시 적용 코드

### Tailwind Config (tailwind.config.js)

```js
module.exports = {
  theme: {
    extend: {
      // 타이포그래피 단순화 (5단계)
      fontSize: {
        'label': ['12px', { lineHeight: '1.2' }],
        'caption': ['13px', { lineHeight: '1.4' }],
        'body': ['17px', { lineHeight: '1.6' }],     // 핵심: 16px → 17px
        'h2': ['24px', { lineHeight: '1.35' }],
        'h1': ['36px', { lineHeight: '1.2' }],
      },
      
      // 컬러 시맨틱 강화
      colors: {
        'dotting-success': '#0D9488',  // Ocean Teal (기능적 성공)
        'dotting-brand': '#F59E0B',    // Warm Amber (브랜드/점)
        'dotting-primary': '#1A365D',  // Deep Navy (행동 유도)
        'dotting-caption': '#4B5563',  // 진한 회색 (캡션용)
      }
    }
  }
}
```

### CSS 변수 업데이트 (globals.css)

```css
:root {
  /* v1.1: 컬러 역할 명확화 */
  --dotting-success: #0D9488;    /* 성공 상태 전용 */
  --dotting-caption-gray: #4B5563; /* 캡션용 진한 회색 */
  
  /* v1.1: 타이포그래피 */
  --font-size-body: 17px;        /* 기존 16px → 17px */
  --line-height-body: 1.6;       /* 유지 */
}

/* v1.1: AI 말풍선 개선 */
.dotting-bubble-ai {
  background-color: var(--dotting-ai-bubble);
  border: 1px solid var(--dotting-border); /* 추가 */
  color: #1F2937; /* 진하게 */
  border-radius: 1rem;
  border-top-left-radius: 0.25rem;
}

/* v1.1: 사용자 말풍선 대비 강화 */
.dotting-bubble-user {
  background-color: var(--dotting-deep-navy); /* 변경 */
  color: white; /* 변경 */
  border-radius: 1rem;
  border-top-right-radius: 0.25rem;
}
```

### Button Component (button.tsx) - v1.5 최종

```tsx
// v1.5: 실제 랜딩 네비게이션 기준 (더 작게)
const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-xl font-medium transition-all duration-200 disabled:pointer-events-none disabled:cursor-not-allowed outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-[var(--dotting-warm-amber)]",
  {
    variants: {
      variant: {
        // Primary CTA - Deep Navy (화면당 1개 원칙)
        default: 
          "bg-[var(--dotting-deep-navy)] text-white font-semibold shadow-sm hover:bg-[#2A4A6F] hover:-translate-y-0.5 hover:shadow-md active:scale-[0.98]",
        
        // Ghost - 비강조 액션
        ghost:
          "bg-transparent text-[var(--dotting-muted-gray)] font-normal hover:text-[var(--dotting-deep-navy)] hover:bg-gray-100/50",
        
        // Celebration - 성취 모먼트만
        celebration:
          "bg-gradient-to-r from-amber-500 to-orange-500 text-white font-semibold shadow-lg hover:from-amber-600 hover:to-orange-600 hover:-translate-y-0.5 hover:shadow-xl active:scale-[0.98]",
        
        // Outline - 보조 액션
        outline:
          "border border-[var(--dotting-border)] bg-transparent text-[var(--dotting-deep-navy)] hover:bg-[var(--dotting-warm-gray)]",
      },
      size: {
        // v1.5: 실제 랜딩 기준 (더 작게)
        sm: "h-8 px-3 text-sm",           // 32px, 14px - Navigation
        default: "h-10 px-5 text-sm",     // 40px, 14px - 일반 액션
        lg: "h-11 px-6 text-base",        // 44px, 16px - Section CTA
        xl: "h-12 px-8 text-base",        // 48px, 16px - Hero CTA
        icon: "size-10",
        "icon-sm": "size-8",
        "icon-lg": "size-11",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
)
```

#### 크기 선택 가이드 (v1.5 최종)

| 용도 | Size | 높이 | 폰트 | 예시 |
|------|------|------|------|------|
| **Navigation** | `sm` | 32px | 14px | "도팅 스토리", "로그인" |
| **일반 액션** | `default` | 40px | 14px | "전송", "취소", "저장" |
| **Section CTA** | `lg` | 44px | 16px | "무료 샘플 보기", "시작하기" |
| **Hero CTA** | `xl` | 48px | 16px | "지금 시작하기" (랜딩 메인) |

#### 중요 원칙

1. **본문 텍스트 ≠ 버튼 텍스트**
   - 본문(읽기용): 17px
   - 버튼(액션용): 14-16px (크기에 따라)
   - **더 작은 버튼 = 더 세련됨**

2. **size prop 명시 필수**
   - 모든 `<Button>` 컴포넌트는 반드시 `size` prop 명시
   - 예: `<Button size="default">전송</Button>`

3. **드롭다운 > 모달**
   - 삭제 같은 위험 행동: 버튼 근처 드롭다운 카드
   - 맥락 유지, 움직임 최소화
   - Red 600 사용으로 위험 명확히 표시
```

### DeleteSessionButton - 드롭다운 UX

```tsx
// v1.5: 중앙 모달 → 드롭다운 카드
// 장점:
// 1. 버튼 근처 표시 → 맥락 유지
// 2. 움직임 최소화 → 안정적
// 3. 작고 절제된 디자인 → 프리미엄

<div className="relative">
  {/* 삭제 아이콘 버튼 */}
  <button className="p-2 rounded-lg hover:bg-gray-100">
    <TrashIcon />
  </button>

  {/* 드롭다운 확인 카드 */}
  {showConfirm && (
    <div className="absolute right-0 top-full mt-2 w-72 
                    bg-white rounded-xl shadow-2xl border p-4 z-50">
      <h4>정말 삭제하시겠어요?</h4>
      <p className="text-xs">프로젝트명의 모든 데이터가 영구 삭제됩니다.</p>
      
      <div className="flex gap-2">
        <button className="flex-1 h-9">취소</button>
        <button className="flex-1 h-9 bg-red-600">삭제</button>
      </div>
    </div>
  )}
</div>
```

---

## 부록 C: v1.5 체크리스트 (최종 수정)

```
✅ button.tsx 크기 체계 최종 수정 (sm: 32px, default: 40px, lg: 44px, xl: 48px)
✅ 모든 Button 컴포넌트에 size prop 명시
✅ DeleteSessionButton 드롭다운 UX로 전면 개편
  - 중앙 모달 → 버튼 근처 드롭다운 카드
  - 아이콘 버튼 (쓰레기통)
  - Red 600 삭제 버튼 (위험 명확화)
  - 외부 클릭 감지로 자동 닫힘
✅ 대화방 "전송" 버튼 크기 수정 (size="default" = 40px)
✅ "이야기 정리하기" 버튼 크기 유지 (size="default")
✅ 랜딩 페이지 버튼 크기 검증 (sm: 32px, xl: 48px)
✅ Auth 페이지 버튼 크기 수정 (size="default" = 40px)
✅ Dashboard 페이지 버튼 크기 수정 (size="default")
✅ PaymentModal 버튼 크기 수정 (size="default")
✅ UX/UI Guidelines 문서 업데이트 (v1.5)

□ 전체 페이지 시각적 검증 (랜딩/대시보드/대화방)
□ 드롭다운 위치/동작 테스트 (다양한 화면 크기)
□ 모바일 반응형 테스트 (iPhone SE 등)
□ 버튼 크기 일관성 최종 확인
```

## 부록 D: v1.1 체크리스트

```
□ Tailwind Config 업데이트 (fontSize, colors)
□ globals.css 업데이트 (말풍선, 변수)
□ button.tsx 업데이트 (Variants 단순화)
□ 기존 코드에서 text-base → text-[17px] 변경
□ 기존 코드에서 text-sm (캡션) → text-[13px] + text-[#4B5563] 변경
□ secondary 버튼 → ghost 버튼으로 변경
□ 점(●●●) 시스템 진행 상태바로 적용
□ 성공 피드백 Ocean Teal 사용 확인
□ 모바일 테스트 (17px 가독성 확인)
□ 말풍선 대비 테스트 (야외/저전력 모드)
```

---

## 부록: v1.1 → v1.2 변경 요약

### A.1 핵심 철학 변화

| 항목 | v1.1 | v1.2 |
|------|------|------|
| **점 시스템** | 진행 상태바 | **패키지 정체성 시스템** |
| **애니메이션** | Bounce (경쾌) | **Ink Spread (묵직)** |
| **Heritage 전략** | 없음 | **Micro-elements 차별화** |
| **오디오 UI** | 없음 | **파형 시각화 + 티저 마킹** |

### A.2 절제된 설계 원칙 (신규)

```
"우리는 게임이 아니라 유산을 만든다"

1. 전환하지 말고, 심화시켜라
   ❌ data-theme="heritage" 전체 테마 전환
   ✅ Micro-elements만 차별화 (점, 배지, 그림자)

2. 폭발이 아니라, 스며듦이다
   ❌ Golden Glow 폭발, 파티클 효과
   ✅ Ink Spread (0.6s, 묵직한 안착)

3. 브랜드의 일관성 사수
   ❌ 새 컬러 도입 (Champagne Gold)
   ✅ 기존 Amber로 금빛 표현
```

### A.3 기술적 구현 변경

| 구성 요소 | 구현 방법 | 파일 |
|----------|----------|------|
| **PackageType Enum** | TypeScript 타입 정의 | `dot.tsx` |
| **Ink Spread** | CSS `@keyframes` + cubic-bezier | `globals.css` |
| **Heritage 금박** | Amber 그라디언트 + SVG 노이즈 | `globals.css` |
| **오디오 파형** | Canvas/SVG 시각화 | `AudioMessage.tsx` |
| **티저 마킹** | `isHighlight` 플래그 + 별 아이콘 | `AudioMessage.tsx` |
| **VIP 배지** | `.heritage` 클래스 + 조건부 렌더링 | `DashboardHeader.tsx` |

### A.4 구현 우선순위

```
🔥 최우선 (v1.2 핵심)
1. Dot.tsx (PackageType Enum)
2. globals.css (Ink Spread + Heritage 변수)
3. AudioMessage.tsx (파형 시각화)

⭐ 높음 (차별화 요소)
4. Heritage 차별화 (VIP 배지, 그림자)
5. 티저 마킹 UI (별표 + 토스트)

✅ 기본 (UX 개선)
6. 광학적 정렬 시스템
7. 터치 최적화 (haptic)
```

---

## 11. 아이콘 및 이모지 사용 원칙

### 11.1 기본 철학

**"절제된 시각 언어"**

DOTTING은 프리미엄 피지털 서비스로서, 시각적 요소는 명품 브랜드의 절제미를 따릅니다. 이모지는 신중하게 선별하여 사용하며, 과도한 장식은 브랜드 품격을 저하시킵니다.

### 11.2 이모지 사용 금지 원칙

**절대 사용 금지**:
- 🎉🎊✨💫🌟⭐ (과도한 축하/반짝임)
- 😀😃😄😁😆 (과장된 표정)
- 👍👏🙌💪 (제스처)
- 🔥💯💝💖 (강조/감정 과잉)
- 📦📁📂💾 (일반적인 파일/저장 아이콘)

**이유**: 
- 캐주얼하고 가벼운 느낌 → 유산 기록의 무게감 상실
- 세대 간 해석 차이 → 시니어에게 혼란 유발
- 디지털 네이티브 전용 언어 → 유니버설 디자인 위배

### 11.3 허용되는 시각 요소

**1. 시스템 아이콘 (우선 사용)**
```
재생/정지: ▶ ⏸ (Unicode 기하학 도형)
체크: ✓ (단순 체크마크)
점: ● ○ (브랜드 아이덴티티)
화살표: → ← ↑ ↓ (방향 표시)
```

**2. 제한적 이모지 (맥락 필수)**
```
🎵 음표: 오디오 관련 기능에만 사용
📖 책: 실물 제품(책) 직접 지칭 시만 사용
```

**사용 조건**:
- 기능을 직접적으로 표현하는 경우에만
- 텍스트 레이블과 반드시 병기
- 단독 사용 금지 (접근성)

### 11.4 Heritage 패키지 전용 시각 언어

**금박 효과 표현**:
- 이모지 사용 금지
- CSS 그라디언트 + box-shadow로 구현
- Champagne Gold (#FCD34D) 톤 사용
- 최대 opacity 0.35 (과도한 반짝임 방지)

**예시**:
```
❌ 나쁜 예: "✨ Heritage 패키지 ✨"
✅ 좋은 예: "Heritage 패키지" (금박 효과는 CSS로)
```

### 11.5 UI 텍스트 작성 원칙

**감성 전달 방법**:
- 이모지 대신 정제된 한글 사용
- 예: "목소리를 영원히 간직하기" (📦 없이)
- 예: "소중한 순간이 완성되었습니다" (✨ 없이)

**톤앤매너**:
- 조용한 럭셔리 (Quiet Luxury)
- 과장 없는 진정성
- 명품 브랜드의 절제된 언어

### 11.6 예외 상황

**허용 가능한 경우**:
1. 사용자 생성 콘텐츠 (UGC)
   - 사용자가 입력한 이모지는 그대로 표시
   - 시스템이 추가하지 않음

2. 에러/경고 메시지
   - ⚠ (경고 삼각형): 시스템 표준 아이콘
   - ✓ (체크): 성공 표시

3. 개발/디버그 환경
   - 내부 문서, 주석에서는 자유롭게 사용 가능
   - 프로덕션 UI에는 절대 노출 금지

### 11.7 검토 체크리스트

**UI 구현 전 확인사항**:
- [ ] 이모지 없이도 의미 전달이 명확한가?
- [ ] 시니어 사용자가 이해 가능한가?
- [ ] 브랜드의 프리미엄 이미지를 유지하는가?
- [ ] 접근성 도구(스크린 리더)가 올바르게 읽는가?
- [ ] 10년 후에도 촌스럽지 않을 디자인인가?

---

## 12. 디지털 유산 상자 (v4.3)

### 12.1 99% 봉인 연출 (Ink Spread 철학)

**개념**:
- 99%에서 1.5초 대기 → "정성껏 봉인 중"
- 마지막 1% 묵직하게 (800ms, Cubic-bezier)
- 완료 순간 Heavy 햅틱 → 물리적 완성감

**구현**:
```typescript
// 99% 도달 시
animateProgress(progress, 99, 800)
await delay(1500)  // 봉인 여운
animateProgress(99, 100, 800)
triggerHaptic('heavy')
```

### 12.2 적응형 폴링 (Adaptive Polling)

**간격 전략**:
- 0-30%: 0.5초 (초반 불안 → 빠른 피드백)
- 30-80%: 1초 (중반 안정 → 표준)
- 80-100%: 2초 (후반 완료 임박 → 서버 부하 감소)

**예상 시간 표시**:
- 5초 단위 반올림
- 보간 처리 (Lerp 0.3) → 툭툭 끊기지 않음
- Fade 전환 (300ms)

### 12.3 3단계 재시도 (우아한 후퇴)

#### 1차 실패: 자동 재시도
```
"잠시 문제가 있었어요. 다시 시도할게요"
→ 2초 대기 후 자동 재시도
→ 사용자 인지 최소화
```

#### 2차 실패: 수동 재시도
```
"상자가 조금 무거워 시간이 걸리네요"
→ [나중에 받기] [다시 시도하기]
→ 사용자 선택권 제공
```

#### 3차 실패: 이메일 대안
```
"상자가 아주 무겁고 소중하여,
저희가 정성껏 포장해 메일로 배달해 드릴까요?"
→ [닫기] [📧 메일로 받기]
→ 실패를 프리미엄 컨시어지 서비스로 전환
```

**핵심**: 실패가 아닌 배려로 느껴지도록

### 12.4 Guide.pdf 구조

**내용**:
1. 사용법 (3단계)
2. 유산 전달 철학
3. 브랜드 메시지
4. 메모란: "가족과 함께 목소리를 들었던 날"

**품질**:
- Noto Serif KR, 300dpi
- A4 1페이지
- 인쇄 최적화

**차별점**:
- 단순 매뉴얼 ❌
- 유산 가이드 ✅

---

*본 문서는 DOTTING 프로젝트의 UX/UI 가이드라인 v1.5입니다.*  
*"절제된 설계" 원칙으로 PRD v4.0을 완전히 반영합니다.*  
*SSOT: `globals.css`, `button.tsx`, `dot.tsx`에서 관리됩니다.*  
*변경 시 이 문서와 코드 양쪽을 업데이트해주세요.*

**v4.3 업데이트 (2026-01-18)**:
- 디지털 유산 상자 시스템 완성
- 스트리밍 압축 (archiver)
- 적응형 폴링 및 진행률 표시
- 3단계 재시도 + 이메일 대안
- Guide.pdf 구조 확정
